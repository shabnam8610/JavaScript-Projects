// This function demonstrates the += operator.
function myFunction() {

    // Start with the first part of the sentence.
    let sentence = "I am learning";

    // The += operator adds more text to the existing string.
    sentence += " JavaScript functions!";

    // Display the completed sentence in the HTML element.
    document.getElementById("concatenate").innerHTML = sentence;
}
